This is the project that i build while learning React.js

steps to put your code on github:-
//initialise a local git repo
1 git init
//internal work
2 git status
3 git add .
4 git commit -m "some msg"
// create new repo on github
5 git remote add origin created_repo_link
6 git push -u origin main
//if u use -u ...next time just do => git push

Author: me
